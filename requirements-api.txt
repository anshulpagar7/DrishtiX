# Backend. CPU-only - the API runs without a GPU, falling back to the
# classical baselines until a trained adapter is present.
-r requirements.txt

fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6

# The trained model itself needs training/requirements.txt on a GPU box.
